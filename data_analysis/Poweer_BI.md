# Power BI

## 00_界面认识与矩阵表操作

1. 整体大小：大小
2. 行高：行填充
3. 列宽：列宽
4. 隐藏列：先关掉【自动换行】，再用鼠标拖拽
5. 隐藏行：筛选器里面进行筛选
6. 居中显示：【行标题】与【字段格式设置】中分别居中
7. 分级显示：+
8. 行标题缩进：缩进
9. 行字段名显示在不同列：渐变
10. 行总计：小计 【按行级别，产品名称】
11. 值在行上显示：值
12. 条件格式：数据条
13. 条件格式：图标



## 01_运算符与新建列

### 运算符

| 运算符 | 含义 | 运算符 |      | 运算符 | 含义       |
| ------ | ---- | ------ | ---- | ------ | ---------- |
| +      | 加   | =      |      | &&     | 与         |
| -      | 减   | >      |      | \|\|   | 或         |
| *      | 乘   | <      |      | not    | 非         |
| /      | 除   | >=     |      | &      | 字符串连接 |
| ^      | 幂   | <=     |      | in     | 包含       |
|        |      | <>     |      | not in | 不包含     |

### 新建列

`新列 = [销售数量]*10`

`新列 = '销售表'[销售数量]*10`

## 函数

| 函数名                    | 描述                                                         |
| ------------------------- | ------------------------------------------------------------ |
| LOOKUPVALUE()             | =VLOOKUOP() 条件查找函数                                     |
| RELATED()                 | 联表查询(一端找多端)<br />select 字段名 from 表一 left join 表二 |
| COUNTROWS(RELATEDTABLE()) | 联表查询(多端找一端）<br />select count(*) from 表一 left join 表二 group by 字段名 |
| SUM()                     |                                                              |
| CALCULATE()               | 筛选引擎                                                     |
| CALCULATETABLE()          | 筛选引擎                                                     |















